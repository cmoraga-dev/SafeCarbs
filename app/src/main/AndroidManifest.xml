<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.safecarbs">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".main.accounts.accountViewer"></activity>
        <activity android:name=".main.accounts.ExplanationAccounts" />
        <activity android:name=".main.register.RegisterActivity" />
        <activity android:name=".main.contact.ContactActivity" />
        <activity android:name=".main.choView.ChoViewActivity" />
        <activity android:name=".main.login.LoginActivity" />
        <activity android:name=".main.launcher.LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.search.SearchActivity" />
    </application>

</manifest>